# Frontend – React Application

This folder contains the **frontend** of the Badminton Court Management System, built using **React** and created with **Create React App**.

---

## 📦 Installation

Before running the project, navigate into the frontend directory and install all dependencies:

```bash
npm install
## dùng để lưu thư viện vào dự án 
npm install react-scripts --save
## chạy dự án dùng lệnh
npm start
